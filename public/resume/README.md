# Resume

Place the resume PDF here as `mae-mabanta-resume.pdf`.

This path is referenced by `siteConfig.resumePath` in `lib/site.ts` and is
linked from the "Download Resume" button in the CTA section.
